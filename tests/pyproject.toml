[project]
name = "simulate"
version = "0.1.0"
description = "Test harness for Visez le Maître contest simulator"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]
